spring.application.name=JapanAnimeShop
server.port=8080
# server.servlet.context-path=/JapanAnimeShop

# 配置 jsp
spring.mvc.view.prefix=/WEB-INF/view/
spring.mvc.view.suffix=.jsp

# 配置 Hidden method
spring.mvc.hiddenmethod.filter.enabled=true
# mysql 配置
spring.datasource.url=jdbc:mysql://localhost:3306/shop?useSSL=false&serverTimezone=Asia/Taipei&useLegacyDatetimeCode=false
spring.datasource.username=root
spring.datasource.password=abc123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 配置 錯誤資訊
server.error.whitelabel.enabled=false

# 配置 靜態資源
spring.web.resources.static-locations=classpath:/static/

# 允許多檔案上傳
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# 配置 檔案大小
spring.servlet.multipart.max-file-size=2MB
# Max Request Size
spring.servlet.multipart.max-request-size=2MB

# 配置 Email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# 郵件服務帳密
spring.mail.username=Kevin102041@gmail.com
spring.mail.password=pcqw hvql jwnq ynxs
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

